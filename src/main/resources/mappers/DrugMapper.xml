<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.middlepj.ict05.domain.drug.dao.DrugDAO"><!-- namespace="패키지명.인터페이스" -->
	
	<!-- 검색 시 총 개수 -->
	<select id="drugSearchCnt" parameterType="java.util.Map" resultType="int">
		SELECT COUNT(*) AS cnt
		  FROM DRUG_TB
		 WHERE DR_PRODUCT LIKE '%' || #{keyword} || '%'
	</select>
	
	<!-- 검색 시 총 목록 -->
	<select id="drugSearchList" parameterType="java.util.Map" resultType="com.middlepj.ict05.domain.drug.dto.DrugDTO">
		SELECT * 
  		  FROM (SELECT A.*
             		 , rownum AS rn 
          		  FROM (SELECT * 
                 		  FROM DRUG_TB 
                 		 WHERE DR_SHOW = 'Y'
                 		   AND DR_PRODUCT LIKE '%' || #{keyword} || '%' 
                 		 ORDER BY DR_ID DESC) A)  
         WHERE rn BETWEEN #{start} AND #{end}	
	</select>
	
	<!-- 영양제 개수 -->
	<select id="drugCnt" resultType="int">
		SELECT COUNT(*) AS cnt FROM DRUG_TB
	</select>
	
	<!-- 영양제 목록 -->
	<select id="drugList" parameterType="java.util.Map" resultType="com.middlepj.ict05.domain.drug.dto.DrugDTO"> 
		SELECT * 
  		  FROM (SELECT A.*
             		 , rownum AS rn 
          		  FROM (SELECT * 
                 		  FROM DRUG_TB 
                 		 WHERE DR_SHOW = 'Y' 
                 		 ORDER BY DR_ID DESC) A)  
         WHERE rn BETWEEN #{start} AND #{end}
	</select>
	
	<!-- 영양제 중복 확인 -->
	<select id="existCnt" parameterType="java.util.Map" resultType="int">
		SELECT COUNT(*)
		  FROM MEMBER_DRUG_TB
		 WHERE MB_ID = #{mb_id}
		   AND DR_ID = #{dr_id}
	</select>
	
	<!-- 영양제 정보 담기 -->
	<select id="getDrugById" resultType="com.middlepj.ict05.domain.drug.dto.DrugDTO">
		SELECT DR_ID, DR_PRODUCT, DR_MAIN_FUNCTION
		  FROM DRUG_TB
		 WHERE DR_ID = #{dr_id}
	</select>
	
	<!-- 영양제 추가 -->
	<insert id="addDrug" parameterType="java.util.Map">
		INSERT INTO MEMBER_DRUG_TB (MBD_ID, 
									MB_ID, 
									DR_ID, 
								    MBD_DRUG_NAME, 
								    MBD_DRUG_EFFECT, 
								    MBD_WRITER_ID, 
								    MBD_REG_DATE)
		 VALUES((SELECT NVL(max(MBD_ID)+1, 1)FROM MEMBER_DRUG_TB), 
		 		#{mb_id}, 
		 		#{dr_id}, 
		 		#{mbd_drug_name}, 
		 		#{mbd_drug_effect}, 
		 		#{mb_id}, 
		 		SYSDATE)
		  
	</insert>
	
	<!-- 영양제 상세 페이지 -->
	
	
</mapper> 